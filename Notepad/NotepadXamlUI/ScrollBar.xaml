<UserControl x:ConnectionId='1'
    x:Class="NotepadXamlUI.ScrollBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotepadXamlUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ScrollBarBackgroundBrush}"
                               
    mc:Ignorable="d">
    <ScrollViewer x:ConnectionId='2' HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  ZoomMode="Enabled"
                  IsVerticalScrollChainingEnabled="True"
                  Background="{ThemeResource ScrollBarBackgroundBrush}"
                                                          
                  x:Name="ScrollViewer">
        <Canvas x:ConnectionId='3' x:Name="ScrollContent"
                Background="Transparent"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Width="5" Height="5"/>
    </ScrollViewer>
    
</UserControl>

